# Design System

To be filled in as UI is built. Will document:

- Tailwind theme tokens (colors, spacing, typography) and any shadcn/ui theme overrides.
- The approved set of shadcn/ui components used across the app.
- Card and list layout conventions (AlumniCard / VolunteerCard / JobCard).
- Accessibility notes: contrast ratios, focus states, keyboard navigation.
